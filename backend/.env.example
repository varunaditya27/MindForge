# Backend env example for Render/Local
# Copy to .env and fill in values

# FastAPI
DEBUG=false
HOST=0.0.0.0
PORT=8000

# CORS: allow your Vercel domain(s)
# Comma-separated exact origins
CORS_ORIGINS=http://localhost:5173,https://<your-vercel-app>.vercel.app
# Optional regex to allow Vercel preview URLs
# Example matches https://idea-arena-<hash>-<user>.vercel.app
# CORS_ALLOW_ORIGIN_REGEX=^https:\/\/.*vercel\.app$

# Gemini (single key) or multi-key (comma-separated for round-robin)
GEMINI_API_KEY=
# Optional: multiple keys for event day load, will be used in round-robin
# Example: GEMINI_API_KEYS=key1,key2,key3
GEMINI_API_KEYS=

# Google Programmable Search Engine (free tier)
# Create: https://programmablesearchengine.google.com/
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=

# Firebase (Admin SDK) - split credentials only
FIREBASE_PROJECT_ID=
FIREBASE_TYPE=service_account
FIREBASE_PRIVATE_KEY_ID=
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
FIREBASE_CLIENT_EMAIL=
FIREBASE_CLIENT_ID=
FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token
FIREBASE_AUTH_PROVIDER_X509_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
FIREBASE_CLIENT_X509_CERT_URL=
FIREBASE_UNIVERSE_DOMAIN=googleapis.com
