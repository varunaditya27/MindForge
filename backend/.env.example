# Backend env example for Render/Local
# Copy to .env and fill in values

# FastAPI
DEBUG=false
HOST=0.0.0.0
PORT=8000

# CORS: allow your Vercel domain(s)
# Comma-separated exact origins
CORS_ORIGINS=http://localhost:5173,https://<your-vercel-app>.vercel.app
# Optional regex to allow Vercel preview URLs
# Example matches https://idea-arena-<hash>-<user>.vercel.app
# CORS_ALLOW_ORIGIN_REGEX=^https:\/\/.*vercel\.app$

# Gemini
GEMINI_API_KEY=

# Firebase (Admin SDK)
FIREBASE_PROJECT_ID=
# Option 1: Path to service account JSON file (local dev default: ./firebase_admin_sdk.json)
FIREBASE_SERVICE_ACCOUNT_KEY=./firebase_admin_sdk.json
# Option 2: Paste the entire JSON (safest for Render); keep quotes and escapes intact
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account", ...}
